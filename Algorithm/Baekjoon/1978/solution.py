# 백준 1978번 - 소수 찾기

# 수의 개수 N 입력
N = int(input())

# N개의 수 입력
# 슬라이싱으로 N개의 수만 입력되도록 강제제한
numbers = list(map(int, input().split())) [:N]

# 입력값에서 소수가 몇 개인지 구하기
count = 0 # 소수의 개수를 체크할 변수

for i in numbers :
    if i

